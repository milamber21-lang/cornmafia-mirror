// FILE: apps/web/src/app/page.tsx
export default function Home() {
  return (
    <section className="card">
      <h1>Quiet power. Clean surface.</h1>
      <p>
        Base shell is ready. The entire site is governed by these CSS tokens. CMS content will render inside our 
        <span> </span>
        <code>.prose</code> wrapper without custom inline styles.
      </p>
      <p>
        Links are <a href="#sample">brand red</a>. We will layer the minimal landing and Docs/News next.
      </p>
    </section>
  );
}
